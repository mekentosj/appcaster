<%- bootstrap.selectField(channel, 'app_id', apps, { name: 'channel', label: 'App' }) %>
<%- bootstrap.textField(channel, 'title', { name: 'channel', label: 'Title' }) %>
<%- bootstrap.textField(channel, 'url_slug', { name: 'channel', label: 'URL' }) %>
<%- bootstrap.textField(channel, 'language', { name: 'channel' }) %>
<%- bootstrap.textField(channel, 'platform', { name: 'channel' }) %>
<%- bootstrap.textareaField(channel, 'description', { name: 'channel' }) %>
